# Production retrospective — Release 1.0.0

Dated copy of [production-retrospective.md](../production-retrospective.md) after Gate D Hypercare.

| Field | Value |
|-------|-------|
| Release | 1.0.0 |
| Cutover date (UTC) | 2026-07-25T22:05:27Z |
| Hypercare ended (UTC) | 2026-07-26T22:38:24Z |
| Facilitator | Operations / Release (solo operator) |

### What went well

- Gate C evidence packs + Decision Log made Gate D entry unambiguous.
- Traefik Host flip + `api.roamkit.net` DNS + staging Host narrowing completed without P0.
- Rollback window (30m) and Hypercare (24h / 96 samples) ran with only transient probe failures.
- Billing DoD and public catalog/smoke paths confirmed customer-facing stack.

### What we will change

- Keep `ROAMKIT_GIT_SHA` / `ROAMKIT_IMAGE_TAG` aligned with the **API** image OCI revision (close-out found env mirroring web SHA while OCI label was `631e79b…`).
- Prefer pairing API/web tags explicitly in production deploy (avoid `api:main` drifting from recorded Manifest SHAs).
- Retarget Uptime Kuma API monitors to public HTTPS Hosts (13–15 still on Docker-internal URLs at snapshot).
- Restore host `smoke-test-production.sh` to public HTTPS (or document curls + Playwright as canonical).

### Action items

| Action | Owner | Due |
|--------|-------|-----|
| Fix prod `.env` `ROAMKIT_GIT_SHA` / `ROAMKIT_IMAGE_TAG` to API OCI rev | Ops | Next maintenance window |
| Kuma 13–15 → `https://api.roamkit.net/...` | Ops | Next maintenance window |
| Cross-repo `deploy-production` image pairing before routine `api` `main` promotes | Eng | Before next API promote |

## Related

- [gate-d.md](./1.0.0/evidence/gate-d.md)
- [Release Decision Log](../release-decision-log.md)
- [manifest.md](./1.0.0/manifest.md)
