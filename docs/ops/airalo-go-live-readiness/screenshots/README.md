# Screenshots

Wave 1 / sandbox E2E UI proof (staging):

| File | Captures |
|------|----------|
| _(attach)_ | `/plans` catalog browse |
| _(attach)_ | `/me/esims/{id}/setup` wizard (Install step + QR) |
| _(attach)_ | Detail **Continue setup** / post-confirm status |

API/JSON proof lives under `../api-logs/` and `../telemetry/`. Operator may drop PNG/WebP captures here without changing acceptance if JSON evidence is complete.
